<%=form_with url: measurements_path, method: :get do |f| %>
  <%=f.label "Search by quantity"%>
  <%=f.text_field :search%>
  <%=f.submit :search%>
<%end%>

<%=render partial: "measurements", locals: {measurements: @measurements, index: true}%>
